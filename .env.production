# DO NOT commit real secrets to this file.
# Set DISCORD_TOKEN here only on the server/volume — never in Git.
# Railway users: set variables in the Railway dashboard (Variables tab)
# and they will be injected automatically at runtime without this file.
DISCORD_TOKEN=
